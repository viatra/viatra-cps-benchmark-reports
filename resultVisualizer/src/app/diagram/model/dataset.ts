export class Dataset {
    label: string;
    data: Array<{ x: Number, y: number }>;
    hidden: boolean;
    borderColor: string;
    backgroundColor: string;
    fill: boolean;
    lineTension: number;
}
