import { DropDownDirective } from './drop-down.directive';

describe('DropDownDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDownDirective(null);
    expect(directive).toBeTruthy();
  });
});
